import { createBrowserRouter } from "react-router-dom";
import MainRoutes from "./MainRoutes";

const router = createBrowserRouter(MainRoutes); // Ensure it's passed correctly

export default router;
